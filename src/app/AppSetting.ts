export class AppSettings {
    private static IP_ADD = "https://localhost:7287"
    public static readonly API_GRAPHQL_ENDPOINT=this.IP_ADD + '/graphql';
    public static readonly API_USERS_URL=this.IP_ADD + '/api/users';
}

